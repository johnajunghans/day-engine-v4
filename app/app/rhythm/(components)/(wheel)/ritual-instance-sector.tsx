import { RitualInstance } from "@/lib/types/rhythm-types"

interface RitualInstanceSectorProps {
    svgSize: number
    instance: RitualInstance
}

export default function RitualInstanceSector({ }: RitualInstanceSectorProps) {
    return (
        <g>

        </g>
    )
}